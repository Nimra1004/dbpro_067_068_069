@model IEnumerable<DB55.ViewModel.DiseasesModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        
        <td>
            @Html.ActionLink("Add Reason", "AddReason", new {id=item.Id}) |
            @Html.ActionLink("Add Symptoms", "AddSymptom", new { id = item.Id }) |
            @Html.ActionLink("Add Treatement", "AddMedicine", new { id = item.Id })
        </td>
    </tr>
}

</table>
